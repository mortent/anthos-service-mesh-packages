apiVersion: install.istio.io/v1alpha1
kind: IstioOperator
metadata:
  name: cni-onprem
spec:
  components:
    cni:
      namespace: kube-system
      enabled: true
  values:
    cni:
      cniBinDir: /opt/cni/bin
      excludeNamespaces:
        - istio-system
        - kube-system
        - gke-system
        # [END docs]
# [END docs]
